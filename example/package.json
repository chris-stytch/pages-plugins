{
  "name": "pages-plugins-example",
  "private": true,
  "version": "1.0.0",
  "dependencies": {
    "@cfpreview/pages-plugins-stytch": "file:../packages/stytch",
    "graphql": "^16.3.0"
  },
  "scripts": {
    "clean": "rm -f static/_worker.js",
    "build:prod": "npx wrangler pages functions build --outfile static/_worker.js",
    "start": "npx wrangler pages dev static"
  }
}
