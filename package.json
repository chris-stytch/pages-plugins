{
  "name": "pages-plugins",
  "private": true,
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "prebuild:prod": "npm run build",
    "build:prod": "npm run build:prod --workspace=./packages/example",
    "prestart": "npm run build",
    "start": "npm run start --workspace=./packages/example"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^4.6.3",
    "wrangler": "^0.0.0-b3ad07d"
  }
}
